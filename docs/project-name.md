# Project Name

**Audit Management System**

This repository should consistently use the project name above in:

- `.env` templates (`APP_NAME`)
- documentation headers and deployment examples
- package metadata (`composer.json`)

The canonical short name for package metadata is:

`audit-management-system/audit-management-system`
